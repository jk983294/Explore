#!/usr/bin/perl
use strict;
use warnings;

sub main {
    my $num = 42;
    my $str = "hello" . "world";    # Concatenates strings.
    my $mix = $str . $num;          # concatenates string and number.
}

main();
